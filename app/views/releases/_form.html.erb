<%= simple_form_for @release, wrapper: :horizontal_form do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :scope_id %>
  <%= f.input :pillar_id %>

  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
