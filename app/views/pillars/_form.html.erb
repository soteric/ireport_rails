<%= form_for(@pillar) do |f| %>
  <% if @pillar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pillar.errors.count, "error") %> prohibited this pillar from being saved:</h2>

      <ul>
      <% @pillar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
